class AppSpec extends BaseAppSpec {
  init() {
    constraint {
      - there should be only one single php file, {app_entrypoint}, no other php files should be generated
      - after {app_entrypoint} is generated or modified {
        let test_result = TestManager.run()
        if (test_result is false) {
          rerurn AppSpec.init()
        }
      }
    }

    div.book_content.style.background: "wheat"

    layout {
      div.style {
        background: BurlyWood
      } {
        {textbox.book_url}
      }
      div {
        {book_content} 
      }
    }

    div.book_content {
      words.@highlighted {
        show modal.highlighted_words_translation(highlighted words)
      }
  
      paragraphs.each (paragraph) {
        @load {
          add SimpleEnglishConversionButton {
            text: "Convert to Simple English"
            behavior: show when {paragraph} is visible
            params: paragraph.text
            position: append at the end of the {paragraph} instead of below it
          }
        }
      }
    }
  }
}
