requirement {
  please help me generate a cli script to do the following:
  - take the first argument as a question {question} to send to gemini in chrome browser 
  - it should automatically authenticate me using my gmail account and password
}

task (complete) {
  the ./ask_gemini.sh is working great but i want to capture the gemini output in browser and display them in command line
}

task.fix_termini_display (completed){
  the response does not show in terminal, please fix it
}

task (completed){
  task.fix_termini_display is working great, but can we have a "streaming" effect so that i can see the response as soon as it's possible instead of waiting for a long time?
}

task (completed){
  every time when I run ./ask_gemini.sh "my question here", chrome will be open, can we not have chrome open or keep chrome minimized?
}

task (completed) {
  the answer is not right, seems like keeping chrome in the foreground works
  can we have chrome works in foreground a bit but quickly minimized?
}

task (completed){
  it does not work, the answer is not right, i got:
  "Where should we start?"
}

task (completed){
  when i run:
  ./ask_gemini.sh "What is php"       
  i got: "
  missing value
  Question sent to Gemini: What is php
  Waiting for response...

  ============================================
  Gemini Response:
  ============================================
  Hi Jim
  Where should we start?
  ============================================"
  please fix it!!!
}

task (completed){
  i got the following:
  ./ask_gemini.sh "What is php"      
  What is php                                                                                                                                       
  Question sent to Gemini: What is php
  Waiting for response...

  ============================================
  Gemini Response:
  ============================================
  ...........
  (Error: Question does not appear to have been submitted.)
  (Check that Chrome is open and Gemini page loaded correctly.)
  ============================================
}

task (completed){
  after opening the gemini url, record the session id, the session id usually will be https://gemini.google.com/app/{session_id} 
  when asking the next question, resume the session with {session_id}
}

task (completed){
  I have a prompt like the following prompt "
  role: Python 3 and Computer Algorithm Expert

  objective: write a python script to perform bubble sort

  constraint {
    display codeblock only
    no explanation
    no extra words
    proper formatted python code
  } 
  "
  but it does not work, please fix it!
}

task (completed){
  when running: sh ask_gemini.sh -f python_bubblesort_gemini.prompt {
    do not display the prompt in terminal
    just display the result of the prompt
  } 
}

task.no_prompt (completed) {
  when running: 
  sh ask_gemini.sh -f python_bubblesort_gemini.prompt                                 
  i still got: 
  role: Python 3 and Computer Algorithm Expert

  objective: write a python script to perform bubble sort

  constraint {
    display codeblock only
    no explanation
    no extra words
    proper formatted python code
  }def bubble_sort(arr):
  n = len(arr)
  for i in range(n):
  swapped = False
  for j in range(0, n - i - 1):
  if arr[j] > arr[j + 1]:
  arr[j], arr[j + 1] = arr[j + 1], arr[j]
  swapped = True
  if not swapped:
  break
  return arr

  noticed the prompt content still shows up, please fix it!
}

task (completed){
  the task.no_prompt is still not fixed, i still see the prompt content shows up! it should not show the prompt content
}

task {
  after reading the prompt file, get the prompt content, but replace all new lines with "|", the goal is to convert the multi-line string prompt into a single line string prompt    
}
