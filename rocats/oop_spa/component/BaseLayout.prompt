class BaseLayout
{
  BookInput {
    background: wheat
    textbox {
      @key.enter {
        let url = (textbox content)
        bookcontent  = (use php to fetch the content of {url})
        set BookContent.content = {bookcontent}
      }
    }
  }

  BookContent {
    background: light yellow  
  }
}
