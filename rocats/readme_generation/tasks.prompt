task [closed] {
  generate README.md file with the following:
  
  ROCAT is an application framework runs on multiple AI coding agents like copilot-cli, gemini-cli, claudecode... etc 

  Each application running on this framework is called "ROCAT", it stands for:
  Role
  Objective
  Constraint
  Application Specification
  Tasks

  usage: sh rocat.sh <rocat_name>
  example: sh rocat.sh sudoku_spa_app
}

task [closed] {
  In README.md, add a section demonstrating a curly prompt language that is designed to let coding agent understand the logic better.

  Example:
  <code>
  app_spec {
    html {
      component.abc {
        div.id:"abc" {
          text: "This is a abc div"
        }
      }
    }  
  }
  </code>
}

task [closed] {
  add a new section to mention one feature of ROCAT, it is designed to work with any editors, it can good for users who want to do vibe coding in their editor instead of in the coding agent console
}

task [closed] {
  in the "Editor Integration" section, add a new point {
    one advantage of ROCAT is to allow vibe coders see a list of "vibes" (tasks) they completed
  } 
}

task [closed] {
  please include rocat.jpg as the header image
}

task [closed] {
  the rocat.jpg is not found in the README.md
}

task [closed] {
  simply open this task so that we can update spec at app_spec.prompt
}

task [closed] {
  add a new section to mention that ROCAT has ability to define a workflow to constantly merge the changes on each tasks into app_spec.md, reflecting the spec driven development mindset
}

task.remove_image_reference [closed] {
  remove the rocat.jpg reference in readme
}

task [closed] {
  update app_spec.prompt based on task.remove_image_reference
}

task [closed] {
  please include "https://github.com/jimthunderbird/rocat/blob/master/rocat.jpg" as the header image
}
